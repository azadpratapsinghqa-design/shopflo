name: Assignment 2 — Jest API Tests (FakeStoreAPI)

on:
  push:
    paths:
      - 'assignment2/**'
      - '.github/workflows/assignment2.yml'
  pull_request:
    paths:
      - 'assignment2/**'

defaults:
  run:
    working-directory: assignment2

jobs:
  api-tests:
    name: API Tests — Node ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ['18', '20']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: assignment2/package.json

      - name: Install dependencies
        run: npm ci

      - name: Run API test suite
        run: npm test -- --forceExit
        env:
          CI: true

      - name: Upload JUnit results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: api-test-results-node${{ matrix.node-version }}
          path: assignment2/test-results/results.xml
          retention-days: 30